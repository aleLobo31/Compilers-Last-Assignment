main() {
    int contador = 3;
    puts("Iniciando cuenta atras:");
    
    while (contador > 0) {
        printf("Quedan: ", contador);
        contador = contador - 1;
    }
    
    puts("Despegue!");
}